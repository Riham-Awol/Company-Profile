# Nexora — Company Profile

Single-page company profile for a fictional tech company. Product cards link out to each product's own site.

Open `index.html` in a browser — no build step, no dependencies.

Placeholders to replace: company name, stats, the four product URLs (`*.example.com`) and the contact email.
